spring:
    application:
        name: wex-challenge
    jackson:
        date-format: yyyy-MM-dd
    datasource:
        url: jdbc:postgresql://localhost:5432/mydb
        username: postgres
        password: postgres
    jpa:
        hibernate:
            ddl-auto: update
            show-sql: true
        properties:
            hibernate:
                format_sql: true
